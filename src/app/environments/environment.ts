// Este arquivo armazena as configurações para o ambiente de desenvolvimento.
// Você pode, por exemplo, voltar a usar 'http://localhost:8080' aqui
// enquanto mantém a URL de produção no outro arquivo.
export const environment = {
  production: false,
  // Configura a URL para o backend local
  apiUrl: 'http://localhost:8080'
};